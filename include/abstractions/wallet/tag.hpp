#ifndef ABSTRACTIONS_WALLET_TAG
#define ABSTRACTIONS_WALLET_TAG

namespace abstractions {
    
    namespace bitcoin {
        
        // Tag means that we have a way of finding
        // this type of object in a script. 
        struct tag {};
        
    }
}

#endif
