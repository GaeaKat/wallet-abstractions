#include <abstractions/spv/bip37.hpp>

namespace abstractions {
    
    namespace merkle {
        
        list<node<digest>> parse_partial(bytestring b) {
            std::vector<node<digest>>();
        }
        
    }
    
}
